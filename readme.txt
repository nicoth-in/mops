Чтобы запуститься, в package выполняем cargo run;
Код на плюсах изолирован в src/corelib/cpp;
Там же для запуска отдельно выделен файл с загаловками stdc++.h, т.к. он не стандартный для vs;
Rust компилирует для себя сишный код через vs через /build.rs;
